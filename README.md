# codes-and-plots
Codes used in the experiment and plots generated from obtained data

The file name of the yaml file includes 'CALI', however, the frequencies listed in the yaml file are back-up frequencies used when the frequencies of interest where unavailable. 
This file is a concatenation of two yaml-files, each being run in a different CMD prompt

